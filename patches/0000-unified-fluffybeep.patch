diff --git a/lib/controllers/beeper_reinit_controller.dart b/lib/controllers/beeper_reinit_controller.dart
index 44b38a3..5f34c85 100644
--- a/lib/controllers/beeper_reinit_controller.dart
+++ b/lib/controllers/beeper_reinit_controller.dart
@@ -1,5 +1,8 @@
+import 'dart:io';
 import 'package:flutter/material.dart';
 import 'package:matrix/matrix.dart';
+import 'package:fluffychat/widgets/matrix.dart';
+import 'package:shared_preferences/shared_preferences.dart';
 
 class BeeperReinitController {
   final Client client;
@@ -27,7 +30,7 @@ class BeeperReinitController {
       context: context,
       builder: (context) => AlertDialog(
         title: const Text('Re-sincronizar chats (Beeper)'),
-        content: const Text('Se limpiará la caché de chats y perfiles. Para completar el proceso, la aplicación se cerrará y deberás volver a abrirla. ¿Deseas continuar?'),
+        content: const Text('La caché se limpiará y la aplicación se cerrará. Al volver a abrirla, descargará todo tu historial y avatares forzosamente.'),
         actions: [
           TextButton(
             onPressed: () => Navigator.of(context).pop(false),
@@ -78,28 +81,22 @@ class BeeperReinitController {
     Overlay.of(context).insert(overlay);
 
     try {
-      final controller = BeeperReinitController(client: client);
-      await controller.reinitCache(context);
-      
-      // Remove overlay before showing success dialog
+      await client.clearCache();
+      final prefs = await SharedPreferences.getInstance();
+      await prefs.setBool('beeper_needs_cache_refresh', true);
+      await Future.delayed(const Duration(milliseconds: 500));
       overlay.remove();
-
-      // Show success dialog. The user must kill the app manually to ensure
-      // a clean restart — programmatic navigation after clearCache() can
-      // cause widget tree corruption as the client re-initializes.
       if (context.mounted) {
-        await showDialog(
+        showDialog(
           context: context,
           barrierDismissible: false,
           builder: (context) => AlertDialog(
             title: const Text('Caché limpiada'),
-            content: const Text(
-              'La caché se ha limpiado correctamente. Para evitar errores de sincronización, por favor cierra esta aplicación forzadamente desde el menú de aplicaciones recientes de tu teléfono y vuelve a abrirla.',
-            ),
+            content: const Text('Para descargar el historial completo de Beeper, CIERRA ESTA APLICACIÓN forzadamente desde las tareas recientes de tu celular y vuelve a abrirla.'),
             actions: [
               TextButton(
                 onPressed: () => Navigator.of(context).pop(),
-                child: const Text('Aceptar'),
+                child: const Text('Entendido'),
               ),
             ],
           ),
diff --git a/lib/pages/chat/chat_app_bar_title.dart b/lib/pages/chat/chat_app_bar_title.dart
index ad95ef2..c9488c6 100644
--- a/lib/pages/chat/chat_app_bar_title.dart
+++ b/lib/pages/chat/chat_app_bar_title.dart
@@ -45,7 +45,7 @@ class ChatAppBarTitle extends StatelessWidget {
           Hero(
             tag: 'content_banner',
             child: Avatar(
-              mxContent: room.avatar,
+              mxContent: room.beeperAvatarUrl,
               name: room.getBeeperDisplayname(
                 MatrixLocals(L10n.of(context)),
               ),
diff --git a/lib/pages/chat_list/chat_list.dart b/lib/pages/chat_list/chat_list.dart
index b2d2831..7aded77 100644
--- a/lib/pages/chat_list/chat_list.dart
+++ b/lib/pages/chat_list/chat_list.dart
@@ -176,13 +176,10 @@ class ChatListController extends State<ChatList>
       final events = await room.client.database.getEventList(room, limit: 30);
       for (final ev in events) {
         final t = ev.type;
-        if (t == 'm.room.message' ||
-            t == 'm.room.encrypted' ||
-            t == 'm.sticker') {
+        if (t == 'm.room.message' || t == 'm.room.encrypted' || t == 'm.sticker') {
           BeeperBridgeUtils.setLastMessageTime(room.id, ev.originServerTs);
-          if (mounted) {
-            setState(() {});
-          }
+          BeeperBridgeUtils.lastRealMessageCache[room.id] = ev; // NUEVO
+          if (mounted) setState(() {});
           break;
         }
       }
diff --git a/lib/pages/chat_list/chat_list_item.dart b/lib/pages/chat_list/chat_list_item.dart
index bb8cb4b..fc93b68 100644
--- a/lib/pages/chat_list/chat_list_item.dart
+++ b/lib/pages/chat_list/chat_list_item.dart
@@ -45,12 +45,11 @@ class ChatListItem extends StatelessWidget {
 
     final isMuted = room.pushRuleState != PushRuleState.notify;
     final typingText = room.getLocalizedTypingText(context);
-    final lastEvent = BeeperBridgeUtils.getLastRealMessage(room) ?? room.lastEvent;
+    final cachedRealEvent = BeeperBridgeUtils.lastRealMessageCache[room.id];
+    final lastEvent = cachedRealEvent ?? (room.lastEvent?.type.startsWith('com.beeper') == true ? null : room.lastEvent);
     final ownMessage = lastEvent?.senderId == room.client.userID;
-    final isBeeperFakeDM = BeeperBridgeUtils.isFakeDM(room);
-    final directChatMatrixId = isBeeperFakeDM 
-        ? BeeperBridgeUtils.getRealContactFromFakeDM(room)?.id 
-        : room.directChatMatrixID;
+    final isBeeperFakeDM = room.isBeeperFakeDM;
+    final directChatMatrixId = room.directChatMatrixID;
     final isDirectChat = directChatMatrixId != null || isBeeperFakeDM;
     final hasNotifications = room.notificationCount > 0;
     final backgroundColor = activeChat
@@ -68,9 +67,7 @@ class ChatListItem extends StatelessWidget {
         lastEvent != null &&
         room.getState(EventTypes.RoomMember, lastEvent.senderId) == null;
     final space = isBeeperFakeDM ? null : this.space;
-    final mxContent = isBeeperFakeDM 
-        ? (room.avatar ?? BeeperBridgeUtils.getRealContactFromFakeDM(room)?.avatarUrl) 
-        : room.avatar;
+    final mxContent = room.beeperAvatarUrl;
 
     return Padding(
       padding: const EdgeInsets.symmetric(horizontal: 8),
diff --git a/lib/utils/beeper_bridge_utils.dart b/lib/utils/beeper_bridge_utils.dart
index 65b59c5..b732fc7 100644
--- a/lib/utils/beeper_bridge_utils.dart
+++ b/lib/utils/beeper_bridge_utils.dart
@@ -3,83 +3,81 @@ import 'dart:typed_data';
 import 'package:fluffychat/widgets/future_loading_dialog.dart';
 import 'package:flutter/material.dart';
 import 'package:matrix/matrix.dart';
+import 'package:shared_preferences/shared_preferences.dart';
 
 class BeeperBridgeUtils {
-  /// Regular expression to detect Beeper bridge bots.
-  /// Matches pattern like: @whatsappbot:beeper.local
-  static final RegExp beeperBotRegex = RegExp(r'^@.*bot.*:beeper\..*$');
+  // --- INICIO SISTEMA DE CACHÉ PERSISTENTE ---
+  static SharedPreferences? prefs;
 
-
-  /// Checks if a given matrix user ID belongs to a Beeper bridge bot.
-  static bool isBeeperBot(String userId) {
-    return beeperBotRegex.hasMatch(userId);
+  static void initCacheWithPrefs(SharedPreferences sharedPrefs) {
+    prefs = sharedPrefs;
   }
 
-  /// Determines if a room is a "Fake DM" created by Beeper.
-  /// A Fake DM is defined as a room with exactly 3 members:
-  /// 1. The local user (You)
-  /// 2. The real contact
-  /// 3. The Beeper bridge bot
-  static bool isFakeDM(Room room) {
-    // Check 1 (FAST PATH — no member loading needed):
-    // directChatMatrixID comes from account data (m.direct), which is loaded
-    // immediately from the DB before member events. If it points to a Beeper
-    // network user, this is definitively a Fake DM regardless of isDirectChat.
-    final directId = room.directChatMatrixID;
-    if (directId != null && directId.startsWith('@')) {
-      final localpart = directId.split(':').first.substring(1);
-      for (final entry in networkMap.entries) {
-        if (localpart.startsWith('${entry.key}_') || localpart == '${entry.key}bot') {
-          return true;
-        }
-      }
+  static void saveName(String roomId, String name) {
+    if (prefs == null) return;
+    final key = 'beeper_name_$roomId';
+    if (prefs!.getString(key) != name) {
+      prefs!.setString(key, name);
     }
+  }
 
-    // Check 2: Real groups have an explicit name; Beeper fake DMs are anonymous.
-    final nameEvent = room.states['m.room.name'];
-    if (nameEvent != null && nameEvent.isNotEmpty) {
-      final firstName = nameEvent.values.firstOrNull;
-      final roomName = firstName?.content.tryGet<String>('name') ?? '';
-      if (roomName.trim().isNotEmpty) {
-        return false;
-      }
-    }
+  static String? getCachedName(String roomId) {
+    return prefs?.getString('beeper_name_$roomId');
+  }
 
-    // Check 3: m.bridge state event — explicit bridge marker.
-    final hasBridgeEvent = room.states['m.bridge']?.isNotEmpty ?? false;
-    if (hasBridgeEvent && room.isDirectChat) {
-      return true;
+  static void saveAvatar(String roomId, String url) {
+    if (prefs == null) return;
+    final key = 'beeper_avatar_$roomId';
+    if (prefs!.getString(key) != url) {
+      prefs!.setString(key, url);
     }
+  }
 
-    // Check 4: Only direct chats can be Fake DMs via the 3-member heuristic.
-    // Bridged rooms without the isDirectChat flag are already handled above.
-    if (!room.isDirectChat) return false;
+  static String? getCachedAvatar(String roomId) {
+    return prefs?.getString('beeper_avatar_$roomId');
+  }
 
-    // Check 5: Fallback — 3-member structure: local user + bot + real contact.
-    final members = room.getParticipants();
-    if (members.length != 3) {
-      return false;
+  // --- NUEVOS MÉTODOS DE CACHÉ ---
+  static void saveContactId(String roomId, String contactId) {
+    if (prefs == null) return;
+    final key = 'beeper_contact_$roomId';
+    if (prefs!.getString(key) != contactId) {
+      prefs!.setString(key, contactId);
     }
+  }
 
-    bool hasLocalUser = false;
-    bool hasBot = false;
-    bool hasRealContact = false;
-
-    final clientUserId = room.client.userID;
+  static String? getCachedContactId(String roomId) {
+    return prefs?.getString('beeper_contact_$roomId');
+  }
 
-    for (var member in members) {
-      if (member.id == clientUserId) {
-        hasLocalUser = true;
-      } else if (isBeeperBot(member.id)) {
-        hasBot = true;
-      } else {
-        hasRealContact = true;
-      }
+  static void saveIsFakeDM(String roomId, bool isFake) {
+    if (prefs == null) return;
+    final key = 'beeper_isfakedm_$roomId';
+    if (prefs!.getBool(key) != isFake) {
+      prefs!.setBool(key, isFake);
     }
+  }
+
+  static bool? getCachedIsFakeDM(String roomId) {
+    return prefs?.getBool('beeper_isfakedm_$roomId');
+  }
+  // --- FIN SISTEMA DE CACHÉ PERSISTENTE ---
 
-    return hasLocalUser && hasBot && hasRealContact;
+  static final Map<String, Event> lastRealMessageCache = {};
+
+  // Hacemos la detección del bot más tolerante para evitar falsos positivos
+  static bool isBeeperBot(String userId) {
+    final localpart = userId.split(':').first.toLowerCase();
+    return localpart.endsWith('bot') && userId.contains('beeper');
   }
 
+  /// Determines if a room is a "Fake DM" created by Beeper.
+  /// A Fake DM is defined as a room with exactly 3 members:
+  /// 1. The local user (You)
+  /// 2. The real contact
+  /// 3. The Beeper bridge bot
+  static bool isFakeDM(Room room) => room.isBeeperFakeDM;
+
   /// Extracts the real contact from a Fake DM room.
   /// Returns null if it's not a Fake DM or if members haven't loaded yet.
   ///
@@ -519,42 +517,113 @@ class BeeperNetSpec {
 }
 
 extension RoomBeeperExtension on Room {
-  String getBeeperDisplayname(MatrixLocalizations localizations) {
-    // Primary path: room is a confirmed Fake DM
-    if (BeeperBridgeUtils.isFakeDM(this)) {
-      final contact = BeeperBridgeUtils.getRealContactFromFakeDM(this);
-      if (contact != null) {
-        return contact.calcDisplayname();
+  
+  String? get beeperContactId {
+    final memberStates = states['m.room.member'];
+    if (memberStates != null && memberStates.isNotEmpty) {
+      String? contactId;
+      int activeMembers = 0;
+
+      for (final entry in memberStates.entries) {
+        final mxid = entry.key;
+        final membership = entry.value.content.tryGet<String>('membership');
+        if (membership == 'join' || membership == 'invite') {
+          activeMembers++;
+          // Excluirte a ti mismo y al bot de Beeper
+          if (mxid != client.userID && !BeeperBridgeUtils.isBeeperBot(mxid)) {
+            contactId = mxid;
+          }
+        }
       }
 
-      // Members are still loading (race condition during initial sync).
-      // Use the raw directChatMatrixID localpart as a placeholder instead
-      // of falling back to getLocalizedDisplayname() which would show
-      // "Group with [Bot]". The UI will re-render once members are loaded.
-      final directId = directChatMatrixID;
-      if (directId != null) {
-        // e.g. "@whatsapp_12345:beeper.local" → "whatsapp_12345"
-        return directId.split(':').first.replaceAll('@', '');
+      // Si cargó bien, guardamos en caché
+      if (activeMembers > 0 && activeMembers <= 3 && contactId != null) {
+        BeeperBridgeUtils.saveContactId(id, contactId);
+        return contactId;
       }
     }
 
-    // Secondary path: isFakeDM may have returned false during initial load
-    // (e.g. isDirectChat not yet set), but the directChatMatrixID already
-    // contains a Beeper network user ID. Detect and display the raw ID
-    // instead of "Group with..." to avoid the flash.
-    final directId = directChatMatrixID;
-    if (directId != null && directId.startsWith('@')) {
-      final localpart = directId.split(':').first.substring(1);
-      for (final entry in BeeperBridgeUtils.networkMap.entries) {
-        if (localpart.startsWith('${entry.key}_') ||
-            localpart == '${entry.key}bot') {
-          // This is a Beeper bridged room in its loading window.
-          // Return the raw localpart (e.g. "whatsapp_12345") as placeholder.
-          return localpart;
-        }
+    // Retorno ultrarrápido desde la caché si Matrix aún no arrancó
+    return BeeperBridgeUtils.getCachedContactId(id);
+  }
+
+  bool get isBeeperFakeDM {
+    // Si la sala tiene un nombre de grupo puesto por el usuario, no es un DM
+    if (states['m.room.name']?.values.firstOrNull?.content.tryGet<String>('name')?.trim().isNotEmpty == true) {
+      BeeperBridgeUtils.saveIsFakeDM(id, false);
+      return false;
+    }
+
+    final contactId = beeperContactId;
+    if (contactId != null) {
+      BeeperBridgeUtils.saveIsFakeDM(id, true);
+      return true;
+    }
+
+    // Retorno ultrarrápido desde la caché para evitar que la UI cambie de forma
+    final cached = BeeperBridgeUtils.getCachedIsFakeDM(id);
+    if (cached != null) return cached;
+
+    return false;
+  }
+
+  Uri? get beeperAvatarUrl {
+    final avatarEvent = states['m.room.avatar']?.values.firstOrNull;
+    final urlStr = avatarEvent?.content.tryGet<String>('url');
+    if (urlStr != null && urlStr.trim().isNotEmpty) {
+      BeeperBridgeUtils.saveAvatar(id, urlStr);
+      return Uri.tryParse(urlStr);
+    }
+
+    final contactId = beeperContactId;
+    if (contactId != null) {
+      final state = states['m.room.member']?[contactId];
+      final memberAvatar = state?.content.tryGet<String>('avatar_url');
+      if (memberAvatar != null && memberAvatar.trim().isNotEmpty) {
+        BeeperBridgeUtils.saveAvatar(id, memberAvatar);
+        return Uri.tryParse(memberAvatar);
+      }
+    }
+
+    // Rescatar caché
+    final cached = BeeperBridgeUtils.getCachedAvatar(id);
+    if (cached != null) return Uri.tryParse(cached);
+
+    // CORTAFUEGOS: Si sabemos que es un DM pero no tenemos avatar aún,
+    // devolvemos nulo en lugar de "avatar" para evitar que el SDK ponga el logo de WhatsApp/IG.
+    // Esto hará que FluffyChat dibuje un círculo con las iniciales limpiamente.
+    if (isBeeperFakeDM) {
+      return null;
+    }
+
+    return avatar;
+  }
+
+  String getBeeperDisplayname(MatrixLocalizations locals) {
+    final nameEvent = states['m.room.name']?.values.firstOrNull;
+    final name = nameEvent?.content.tryGet<String>('name');
+    if (name != null && name.trim().isNotEmpty) {
+      BeeperBridgeUtils.saveName(id, name);
+      return name;
+    }
+
+    final contactId = beeperContactId;
+    if (contactId != null) {
+      final state = states['m.room.member']?[contactId];
+      final displayname = state?.content.tryGet<String>('displayname');
+      if (displayname != null && displayname.trim().isNotEmpty) {
+        BeeperBridgeUtils.saveName(id, displayname);
+        return displayname;
       }
+
+      final fallback = contactId.split(':').first.replaceAll('@', '');
+      BeeperBridgeUtils.saveName(id, fallback);
+      return fallback;
     }
 
-    return getLocalizedDisplayname(localizations);
+    final cached = BeeperBridgeUtils.getCachedName(id);
+    if (cached != null) return cached;
+
+    return getLocalizedDisplayname(locals);
   }
 }
diff --git a/lib/widgets/cache_refresh_overlay.dart b/lib/widgets/cache_refresh_overlay.dart
new file mode 100644
index 0000000..be6efd9
--- /dev/null
+++ b/lib/widgets/cache_refresh_overlay.dart
@@ -0,0 +1,161 @@
+import 'package:fluffychat/l10n/l10n.dart';
+import 'package:fluffychat/utils/beeper_bridge_utils.dart';
+import 'package:fluffychat/utils/client_download_content_extension.dart';
+import 'package:fluffychat/utils/matrix_sdk_extensions/matrix_locals.dart';
+import 'package:flutter/material.dart';
+import 'package:matrix/matrix.dart';
+import 'package:shared_preferences/shared_preferences.dart';
+
+class CacheRefreshOverlay extends StatefulWidget {
+  final Client client;
+  final SharedPreferences store;
+  final Widget child;
+
+  const CacheRefreshOverlay({
+    required this.client,
+    required this.store,
+    required this.child,
+    super.key,
+  });
+
+  @override
+  State<CacheRefreshOverlay> createState() => _CacheRefreshOverlayState();
+}
+
+class _CacheRefreshOverlayState extends State<CacheRefreshOverlay> {
+  bool _completed = false;
+  int _processedRooms = 0;
+  int _totalRooms = 0;
+  String _currentRoomName = '';
+
+  @override
+  void initState() {
+    super.initState();
+    _startPrewarming();
+  }
+
+  Future<void> _startPrewarming() async {
+    try {
+      // 1. Wait for the initial sync to finish
+      await widget.client.onSync.stream.firstWhere(
+        (s) => widget.client.prevBatch != null,
+      );
+
+      // 2. Fetch and sort rooms by last message (latest first)
+      final rooms = List<Room>.from(widget.client.rooms);
+      rooms.sort((a, b) {
+        final aTime = a.lastEvent?.originServerTs ?? DateTime.fromMillisecondsSinceEpoch(0);
+        final bTime = b.lastEvent?.originServerTs ?? DateTime.fromMillisecondsSinceEpoch(0);
+        return bTime.compareTo(aTime);
+      });
+
+      if (mounted) {
+        setState(() {
+          _totalRooms = rooms.length;
+        });
+      }
+
+      final locals = MatrixLocals(L10n.of(context));
+
+      // 3. Process each room
+      for (final room in rooms) {
+        if (!mounted) break;
+        final roomName = room.getBeeperDisplayname(locals);
+        setState(() {
+          _currentRoomName = roomName;
+        });
+
+        try {
+          await room.getTimeline().then((t) => t.requestHistory(historyCount: 30));
+        } catch (_) {}
+
+        if (room.beeperAvatarUrl != null) {
+          try {
+            await widget.client.downloadMxcCached(room.beeperAvatarUrl!);
+          } catch (_) {}
+        }
+
+        if (mounted) {
+          setState(() {
+            _processedRooms++;
+          });
+        }
+      }
+    } catch (e) {
+      debugPrint('Error during prewarming cache: $e');
+    } finally {
+      if (mounted) {
+        final prefs = await SharedPreferences.getInstance();
+        await prefs.setBool('beeper_needs_cache_refresh', false);
+        setState(() {
+          _completed = true;
+        });
+      }
+    }
+  }
+
+  @override
+  Widget build(BuildContext context) {
+    return Stack(
+      children: [
+        // Always build the main app child first in the background
+        widget.child,
+
+        // If not completed, display the progress overlay on top
+        if (!_completed)
+          Positioned.fill(
+            child: Scaffold(
+              backgroundColor: Colors.black.withOpacity(0.9),
+              body: Center(
+                child: Padding(
+                  padding: const EdgeInsets.symmetric(horizontal: 32.0),
+                  child: Column(
+                    mainAxisAlignment: MainAxisAlignment.center,
+                    children: [
+                      const CircularProgressIndicator(
+                        valueColor: AlwaysStoppedAnimation<Color>(Colors.white),
+                        strokeWidth: 3,
+                      ),
+                      const SizedBox(height: 32),
+                      const Text(
+                        'Sincronizando chats...',
+                        style: TextStyle(
+                          color: Colors.white,
+                          fontSize: 20,
+                          fontWeight: FontWeight.bold,
+                        ),
+                      ),
+                      const SizedBox(height: 12),
+                      Text(
+                        _totalRooms > 0
+                            ? 'Procesando $_processedRooms de $_totalRooms'
+                            : 'Iniciando conexión...',
+                        style: TextStyle(
+                          color: Colors.white.withOpacity(0.7),
+                          fontSize: 14,
+                        ),
+                      ),
+                      if (_currentRoomName.isNotEmpty) ...[
+                        const SizedBox(height: 16),
+                        Text(
+                          _currentRoomName,
+                          style: TextStyle(
+                            color: Colors.white.withOpacity(0.5),
+                            fontSize: 12,
+                            fontStyle: FontStyle.italic,
+                          ),
+                          textAlign: TextAlign.center,
+                          maxLines: 1,
+                          overflow: TextOverflow.ellipsis,
+                        ),
+                      ],
+                    ],
+                  ),
+                ),
+              ),
+            ),
+          ),
+      ],
+    );
+  }
+}
diff --git a/lib/widgets/matrix.dart b/lib/widgets/matrix.dart
index 2130c58..fe63fda 100644
--- a/lib/widgets/matrix.dart
+++ b/lib/widgets/matrix.dart
@@ -35,6 +35,8 @@ import '../pages/key_verification/key_verification_dialog.dart';
 import '../utils/account_bundles.dart';
 import '../utils/background_push.dart';
 import 'local_notifications_extension.dart';
+import 'cache_refresh_overlay.dart';
+import '../utils/beeper_bridge_utils.dart';
 
 class Matrix extends StatefulWidget {
   final Widget? child;
@@ -409,7 +411,20 @@ class MatrixState extends State<Matrix> {
 
   @override
   Widget build(BuildContext context) {
-    return Provider(create: (_) => this, child: widget.child ?? const SizedBox.shrink());
+    final baseChild = Provider(create: (_) => this, child: widget.child ?? const SizedBox.shrink());
+    return FutureBuilder<SharedPreferences>(
+      future: SharedPreferences.getInstance(),
+      builder: (context, snapshot) {
+        if (snapshot.hasData) {
+          BeeperBridgeUtils.initCacheWithPrefs(snapshot.data!);
+
+          if (snapshot.data!.getBool('beeper_needs_cache_refresh') == true) {
+            return CacheRefreshOverlay(client: client, store: snapshot.data!, child: baseChild);
+          }
+        }
+        return baseChild;
+      },
+    );
   }
 
   Future<void> dehydrateAction(BuildContext context) async {
