diff --git a/lib/controllers/beeper_reinit_controller.dart b/lib/controllers/beeper_reinit_controller.dart
index fc5cb33..a0841d8 100644
--- a/lib/controllers/beeper_reinit_controller.dart
+++ b/lib/controllers/beeper_reinit_controller.dart
@@ -99,16 +99,26 @@ class BeeperReinitController {
           barrierDismissible: false,
           builder: (context) => AlertDialog(
             title: const Text('Caché limpiada'),
-            content: const Text('La caché se ha limpiado correctamente. La aplicación se cerrará ahora para aplicar los cambios. Por favor, ábrela nuevamente.'),
+            content: Text(Platform.isIOS
+                ? 'La caché se ha limpiado correctamente. Por favor, cierra la aplicación manualmente (deslizándola hacia arriba en la barra de aplicaciones recientes) y ábrela nuevamente para aplicar los cambios.'
+                : 'La caché se ha limpiado correctamente. La aplicación se cerrará ahora para aplicar los cambios. Por favor, ábrela nuevamente.'),
             actions: [
               TextButton(
-                onPressed: () => exit(0),
+                onPressed: () {
+                  if (Platform.isIOS) {
+                    Navigator.of(context).pop();
+                  } else {
+                    exit(0);
+                  }
+                },
                 child: const Text('Aceptar'),
               ),
             ],
           ),
         );
-        exit(0);
+        if (!Platform.isIOS) {
+          exit(0);
+        }
       }
     } catch (e) {
       overlay.remove();
diff --git a/lib/pages/chat_list/chat_list.dart b/lib/pages/chat_list/chat_list.dart
index 9ea9552..27bc1d3 100644
--- a/lib/pages/chat_list/chat_list.dart
+++ b/lib/pages/chat_list/chat_list.dart
@@ -161,6 +161,20 @@ class ChatListController extends State<ChatList>
         return event.originServerTs;
       }
     }
+    
+    final events = room.timeline?.events;
+    if (events != null && events.isNotEmpty) {
+      for (var i = events.length - 1; i >= 0; i--) {
+        final ev = events[i];
+        final t = ev.type;
+        if (t == 'm.room.message' ||
+            t == 'm.room.encrypted' ||
+            t == 'm.sticker') {
+          return ev.originServerTs;
+        }
+      }
+    }
+
     return room.latestEventReceivedTime;
   }
 
@@ -170,14 +184,21 @@ class ChatListController extends State<ChatList>
         .rooms
         .where(getRoomFilterByActiveFilter(activeFilter))
         .toList();
+
+    // Precalculate timestamps to avoid O(N log N) timeline iteration during sorting
+    final timestamps = <String, DateTime>{};
+    for (final r in rooms) {
+      timestamps[r.id] = _getMessageTimestamp(r);
+    }
+
     rooms.sort((a, b) {
       if (a.isFavourite && !b.isFavourite) return -1;
       if (!a.isFavourite && b.isFavourite) return 1;
       if (a.isLowPriority && !b.isLowPriority) return 1;
       if (!a.isLowPriority && b.isLowPriority) return -1;
 
-      final aTime = _getMessageTimestamp(a);
-      final bTime = _getMessageTimestamp(b);
+      final aTime = timestamps[a.id] ?? DateTime.fromMillisecondsSinceEpoch(0);
+      final bTime = timestamps[b.id] ?? DateTime.fromMillisecondsSinceEpoch(0);
       return bTime.compareTo(aTime);
     });
     return rooms;
diff --git a/lib/pages/chat_list/chat_list_body.dart b/lib/pages/chat_list/chat_list_body.dart
index 5789f55..31f1891 100644
--- a/lib/pages/chat_list/chat_list_body.dart
+++ b/lib/pages/chat_list/chat_list_body.dart
@@ -81,13 +81,8 @@ class ChatListViewBody extends StatelessWidget {
                   spaceDelegateCandidates[room.id] == null,
             )
             .where((room) {
-              if (isVirtualSpace) return true;
-              final net = BeeperBridgeUtils.getRoomNetwork(room);
-              if (net != null) {
-                final contained = Matrix.of(context).store.getStringList('beeper_contained_networks') ?? [];
-                if (contained.contains(net.key)) return false;
-              }
-              return true;
+              if (isVirtualSpace || isVirtualTag) return true;
+              return !BeeperBridgeUtils.isRoomHiddenInInbox(client, room.id);
             })
             .toList();
 
@@ -214,7 +209,7 @@ class ChatListViewBody extends StatelessWidget {
                   Column(
                     mainAxisAlignment: MainAxisAlignment.center,
                     children: [
-                      if ((Matrix.of(context).store.getStringList('beeper_contained_networks') ?? []).isNotEmpty)
+                      if (BeeperBridgeUtils.getBeeperLabels(client).values.any((val) => val is Map && val['isShownInInbox'] == false))
                         Padding(
                           padding: const EdgeInsets.all(16.0),
                           child: Text(
diff --git a/lib/pages/chat_list/navigation_rail.dart b/lib/pages/chat_list/navigation_rail.dart
index 5a22bfe..db1bebc 100644
--- a/lib/pages/chat_list/navigation_rail.dart
+++ b/lib/pages/chat_list/navigation_rail.dart
@@ -53,10 +53,10 @@ class _SpacesNavigationRailState extends State<SpacesNavigationRail> {
               title: Text('Ocultar ${net.name} del menú'),
               onTap: () async {
                 final navigator = Navigator.of(context);
-                final hidden = List<String>.from(store.getStringList('beeper_hidden_networks') ?? []);
+                final hidden = BeeperBridgeUtils.getHiddenNetworks(client);
                 if (!hidden.contains(net.key)) {
-                  hidden.add(net.key);
-                  await store.setStringList('beeper_hidden_networks', hidden);
+                  final newHidden = List<String>.from(hidden)..add(net.key);
+                  await BeeperBridgeUtils.setHiddenNetworks(client, newHidden);
                 }
                 navigator.pop();
                 setState(() {});
@@ -64,24 +64,19 @@ class _SpacesNavigationRailState extends State<SpacesNavigationRail> {
             ),
             ListTile(
               leading: Icon(
-                (store.getStringList('beeper_contained_networks') ?? []).contains(net.key)
+                BeeperBridgeUtils.isNetworkContained(client, net.key, net.name)
                     ? Icons.inbox_outlined
                     : Icons.inbox,
               ),
               title: Text(
-                (store.getStringList('beeper_contained_networks') ?? []).contains(net.key)
+                BeeperBridgeUtils.isNetworkContained(client, net.key, net.name)
                     ? 'Mostrar en listado principal'
                     : 'Contener chats solo aquí',
               ),
               onTap: () async {
                 final navigator = Navigator.of(context);
-                final contained = List<String>.from(store.getStringList('beeper_contained_networks') ?? []);
-                if (contained.contains(net.key)) {
-                  contained.remove(net.key);
-                } else {
-                  contained.add(net.key);
-                }
-                await store.setStringList('beeper_contained_networks', contained);
+                final isContained = BeeperBridgeUtils.isNetworkContained(client, net.key, net.name);
+                await BeeperBridgeUtils.setNetworkContained(client, net.key, net.name, !isContained);
                 navigator.pop();
                 setState(() {});
               },
@@ -125,7 +120,7 @@ class _SpacesNavigationRailState extends State<SpacesNavigationRail> {
               title: const Text('Gestionar redes ocultas'),
               onTap: () {
                 Navigator.of(context).pop();
-                _showManageHiddenNetworksDialog(context, store);
+                _showManageHiddenNetworksDialog(context, client);
               },
             ),
           ],
@@ -134,11 +129,11 @@ class _SpacesNavigationRailState extends State<SpacesNavigationRail> {
     );
   }
 
-  void _showManageHiddenNetworksDialog(BuildContext context, SharedPreferences store) {
+  void _showManageHiddenNetworksDialog(BuildContext context, Client client) {
     showDialog(
       context: context,
       builder: (context) {
-        final hidden = List<String>.from(store.getStringList('beeper_hidden_networks') ?? []);
+        final hidden = BeeperBridgeUtils.getHiddenNetworks(client);
         if (hidden.isEmpty) {
           return AlertDialog(
             title: const Text('Redes ocultas'),
@@ -167,8 +162,8 @@ class _SpacesNavigationRailState extends State<SpacesNavigationRail> {
                     icon: const Icon(Icons.add_circle_outline),
                     onPressed: () async {
                       final navigator = Navigator.of(context);
-                      hidden.remove(netKey);
-                      await store.setStringList('beeper_hidden_networks', hidden);
+                      final newHidden = List<String>.from(hidden)..remove(netKey);
+                      await BeeperBridgeUtils.setHiddenNetworks(client, newHidden);
                       navigator.pop();
                       setState(() {});
                     },
@@ -215,7 +210,7 @@ class _SpacesNavigationRailState extends State<SpacesNavigationRail> {
             }
 
             final networksList = beeperNetworks.values.toList();
-            final hiddenNets = store.getStringList('beeper_hidden_networks') ?? [];
+            final hiddenNets = BeeperBridgeUtils.getHiddenNetworks(client);
             final visibleNetworks = networksList.where((n) => !hiddenNets.contains(n.key)).toList();
             final tagsList = customTags.toList()..sort();
 
diff --git a/lib/pages/settings/patches_settings.dart b/lib/pages/settings/patches_settings.dart
index 593f127..8687f98 100644
--- a/lib/pages/settings/patches_settings.dart
+++ b/lib/pages/settings/patches_settings.dart
@@ -43,8 +43,7 @@ class _SettingsPatchesState extends State<SettingsPatches> {
       ..sort((a, b) => a.name.compareTo(b.name));
     final tagsList = customTags.toList()..sort();
 
-    final contained = store.getStringList('beeper_contained_networks') ?? [];
-    final hiddenNets = store.getStringList('beeper_hidden_networks') ?? [];
+
 
     return Scaffold(
       appBar: AppBar(
@@ -89,8 +88,8 @@ class _SettingsPatchesState extends State<SettingsPatches> {
             )
           else
             ...networksList.map((net) {
-              final isContained = contained.contains(net.key);
-              final isHidden = hiddenNets.contains(net.key);
+              final isContained = BeeperBridgeUtils.isNetworkContained(client, net.key, net.name);
+              final isHidden = BeeperBridgeUtils.getHiddenNetworks(client).contains(net.key);
               return Column(
                 children: [
                   SwitchListTile.adaptive(
@@ -98,13 +97,7 @@ class _SettingsPatchesState extends State<SettingsPatches> {
                     title: Text('Contener ${net.name}'),
                     subtitle: Text('Muestra los chats de ${net.name} solo dentro de su sala virtual en el menú lateral.'),
                     onChanged: (val) async {
-                      final newContained = List<String>.from(contained);
-                      if (val) {
-                        newContained.add(net.key);
-                      } else {
-                        newContained.remove(net.key);
-                      }
-                      await store.setStringList('beeper_contained_networks', newContained);
+                      await BeeperBridgeUtils.setNetworkContained(client, net.key, net.name, val);
                       setState(() {});
                     },
                   ),
@@ -113,13 +106,14 @@ class _SettingsPatchesState extends State<SettingsPatches> {
                     title: Text('Ocultar ${net.name} de la barra lateral'),
                     subtitle: Text('Oculta el ícono de acceso directo de ${net.name} de la izquierda.'),
                     onChanged: (val) async {
-                      final newHidden = List<String>.from(hiddenNets);
+                      final hidden = BeeperBridgeUtils.getHiddenNetworks(client);
+                      final newHidden = List<String>.from(hidden);
                       if (val) {
-                        newHidden.add(net.key);
+                        if (!newHidden.contains(net.key)) newHidden.add(net.key);
                       } else {
                         newHidden.remove(net.key);
                       }
-                      await store.setStringList('beeper_hidden_networks', newHidden);
+                      await BeeperBridgeUtils.setHiddenNetworks(client, newHidden);
                       setState(() {});
                     },
                   ),
@@ -146,19 +140,13 @@ class _SettingsPatchesState extends State<SettingsPatches> {
           else
             ...tagsList.map((tag) {
               final displayName = tag.replaceFirst('u.', '');
-              final isContained = contained.contains(tag);
+              final isContained = BeeperBridgeUtils.isTagContained(client, tag);
               return SwitchListTile.adaptive(
                 value: isContained,
                 title: Text('Contener etiqueta: $displayName'),
                 subtitle: const Text('Muestra estos chats solo dentro de la etiqueta en el menú lateral.'),
                 onChanged: (val) async {
-                  final newContained = List<String>.from(contained);
-                  if (val) {
-                    newContained.add(tag);
-                  } else {
-                    newContained.remove(tag);
-                  }
-                  await store.setStringList('beeper_contained_networks', newContained);
+                  await BeeperBridgeUtils.setTagContained(client, tag, val);
                   setState(() {});
                 },
               );
diff --git a/lib/utils/beeper_bridge_utils.dart b/lib/utils/beeper_bridge_utils.dart
index 7bf25d4..2f5a647 100644
--- a/lib/utils/beeper_bridge_utils.dart
+++ b/lib/utils/beeper_bridge_utils.dart
@@ -269,6 +269,189 @@ class BeeperBridgeUtils {
       );
     }
   }
+
+  /// Get all Beeper labels from Account Data.
+  static Map<String, dynamic> getBeeperLabels(Client client) {
+    try {
+      final accountDataEvent = client.accountData['com.beeper.labels'];
+      if (accountDataEvent != null && accountDataEvent.content is Map) {
+        return Map<String, dynamic>.from(accountDataEvent.content);
+      }
+    } catch (e) {
+      Logs().e('Error reading com.beeper.labels: $e');
+    }
+    return {};
+  }
+
+  /// Save Beeper labels map to Account Data.
+  static Future<void> setBeeperLabels(Client client, Map<String, dynamic> labels) async {
+    try {
+      await client.setAccountData(
+        client.userID!,
+        'com.beeper.labels',
+        labels,
+      );
+    } catch (e) {
+      Logs().e('Error saving com.beeper.labels: $e');
+    }
+  }
+
+  /// Check if a room is hidden from the main inbox due to a label with isShownInInbox = false.
+  static bool isRoomHiddenInInbox(Client client, String roomId) {
+    final labels = getBeeperLabels(client);
+    for (final entry in labels.entries) {
+      final val = entry.value;
+      if (val is Map) {
+        final isShown = val['isShownInInbox'];
+        if (isShown == false) {
+          final rooms = val['rooms'];
+          if (rooms is List && rooms.contains(roomId)) {
+            return true;
+          }
+        }
+      }
+    }
+    return false;
+  }
+
+  /// Helper to get hidden networks using a special '_hidden_networks' key in Beeper labels.
+  static List<String> getHiddenNetworks(Client client) {
+    final labels = getBeeperLabels(client);
+    final hiddenEntry = labels['_hidden_networks'];
+    if (hiddenEntry is Map) {
+      final rooms = hiddenEntry['rooms'];
+      if (rooms is List) {
+        return List<String>.from(rooms);
+      }
+    }
+    return [];
+  }
+
+  /// Helper to set hidden networks using a special '_hidden_networks' key in Beeper labels.
+  static Future<void> setHiddenNetworks(Client client, List<String> hiddenKeys) async {
+    final labels = Map<String, dynamic>.from(getBeeperLabels(client));
+    labels['_hidden_networks'] = {
+      'title': 'Hidden Networks',
+      'rooms': hiddenKeys,
+      'isShownInInbox': false,
+      'createdAt': DateTime.now().millisecondsSinceEpoch ~/ 1000,
+    };
+    await setBeeperLabels(client, labels);
+  }
+
+  /// Check if a network is contained (hidden from inbox).
+  static bool isNetworkContained(Client client, String networkKey, String networkName) {
+    final labels = getBeeperLabels(client);
+    for (final val in labels.values) {
+      if (val is Map && val['title'] == networkName) {
+        return val['isShownInInbox'] == false;
+      }
+    }
+    return false;
+  }
+
+  /// Toggle containment of a network.
+  static Future<void> setNetworkContained(Client client, String networkKey, String networkName, bool contained) async {
+    final labels = Map<String, dynamic>.from(getBeeperLabels(client));
+    String? foundUuid;
+    Map<String, dynamic>? foundLabel;
+
+    for (final entry in labels.entries) {
+      final val = entry.value;
+      if (val is Map && val['title'] == networkName) {
+        foundUuid = entry.key;
+        foundLabel = Map<String, dynamic>.from(val);
+        break;
+      }
+    }
+
+    final netRooms = client.rooms
+        .where((r) => getRoomNetwork(r)?.key == networkKey)
+        .map((r) => r.id)
+        .toList();
+
+    if (foundLabel != null && foundUuid != null) {
+      foundLabel['isShownInInbox'] = !contained;
+      final existingRooms = List<String>.from(foundLabel['rooms'] ?? []);
+      for (final rId in netRooms) {
+        if (contained) {
+          if (!existingRooms.contains(rId)) existingRooms.add(rId);
+        } else {
+          existingRooms.remove(rId);
+        }
+      }
+      foundLabel['rooms'] = existingRooms;
+      labels[foundUuid] = foundLabel;
+    } else if (contained) {
+      final newUuid = 'label_${DateTime.now().millisecondsSinceEpoch}_$networkKey';
+      labels[newUuid] = {
+        'title': networkName,
+        'rooms': netRooms,
+        'isShownInInbox': false,
+        'createdAt': DateTime.now().millisecondsSinceEpoch ~/ 1000,
+      };
+    }
+
+    await setBeeperLabels(client, labels);
+  }
+
+  /// Check if a tag is contained.
+  static bool isTagContained(Client client, String tag) {
+    final labels = getBeeperLabels(client);
+    final displayName = tag.replaceFirst('u.', '');
+    for (final val in labels.values) {
+      if (val is Map && val['title'] == displayName) {
+        return val['isShownInInbox'] == false;
+      }
+    }
+    return false;
+  }
+
+  /// Toggle containment of a custom tag.
+  static Future<void> setTagContained(Client client, String tag, bool contained) async {
+    final labels = Map<String, dynamic>.from(getBeeperLabels(client));
+    final displayName = tag.replaceFirst('u.', '');
+    String? foundUuid;
+    Map<String, dynamic>? foundLabel;
+
+    for (final entry in labels.entries) {
+      final val = entry.value;
+      if (val is Map && val['title'] == displayName) {
+        foundUuid = entry.key;
+        foundLabel = Map<String, dynamic>.from(val);
+        break;
+      }
+    }
+
+    final tagRooms = client.rooms
+        .where((r) => r.tags.keys.contains(tag))
+        .map((r) => r.id)
+        .toList();
+
+    if (foundLabel != null && foundUuid != null) {
+      foundLabel['isShownInInbox'] = !contained;
+      final existingRooms = List<String>.from(foundLabel['rooms'] ?? []);
+      for (final rId in tagRooms) {
+        if (contained) {
+          if (!existingRooms.contains(rId)) existingRooms.add(rId);
+        } else {
+          existingRooms.remove(rId);
+        }
+      }
+      foundLabel['rooms'] = existingRooms;
+      labels[foundUuid] = foundLabel;
+    } else if (contained) {
+      final newUuid = 'label_${DateTime.now().millisecondsSinceEpoch}_${tag.replaceAll(".", "_")}';
+      labels[newUuid] = {
+        'title': displayName,
+        'rooms': tagRooms,
+        'isShownInInbox': false,
+        'createdAt': DateTime.now().millisecondsSinceEpoch ~/ 1000,
+      };
+    }
+
+    await setBeeperLabels(client, labels);
+  }
 }
 
 class BeeperNetwork {
diff --git a/lib/widgets/cache_refresh_overlay.dart b/lib/widgets/cache_refresh_overlay.dart
index 9c91173..234696c 100644
--- a/lib/widgets/cache_refresh_overlay.dart
+++ b/lib/widgets/cache_refresh_overlay.dart
@@ -50,6 +50,20 @@ class _CacheRefreshOverlayState extends State<CacheRefreshOverlay> {
         return event.originServerTs;
       }
     }
+    
+    final events = room.timeline?.events;
+    if (events != null && events.isNotEmpty) {
+      for (var i = events.length - 1; i >= 0; i--) {
+        final ev = events[i];
+        final t = ev.type;
+        if (t == 'm.room.message' ||
+            t == 'm.room.encrypted' ||
+            t == 'm.sticker') {
+          return ev.originServerTs;
+        }
+      }
+    }
+
     return room.latestEventReceivedTime;
   }
 
