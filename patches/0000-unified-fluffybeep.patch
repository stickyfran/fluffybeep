diff --git a/lib/controllers/beeper_reinit_controller.dart b/lib/controllers/beeper_reinit_controller.dart
index 1ef4102..33292ad 100644
--- a/lib/controllers/beeper_reinit_controller.dart
+++ b/lib/controllers/beeper_reinit_controller.dart
@@ -13,11 +13,14 @@ class BeeperReinitController {
   Future<void> reinitCache(BuildContext context) async {
     try {
       // 1. Pause sync loop
       client.backgroundSync = false;
 
-      // 2. Clear the database cache while keeping keys and session
+      // 2. Give the SDK time to stop its background threads before touching the DB
+      await Future.delayed(const Duration(milliseconds: 500));
+
+      // 3. Clear the database cache while keeping keys and session
       await client.database?.clearCache();
     } catch (e) {
       debugPrint('Error during Beeper cache reinitialization: $e');
       rethrow;
     }
diff --git a/lib/utils/beeper_bridge_utils.dart b/lib/utils/beeper_bridge_utils.dart
index d037b5f..a0f0ea1 100644
--- a/lib/utils/beeper_bridge_utils.dart
+++ b/lib/utils/beeper_bridge_utils.dart
@@ -7,18 +7,10 @@ import 'package:matrix/matrix.dart';
 class BeeperBridgeUtils {
   /// Regular expression to detect Beeper bridge bots.
   /// Matches pattern like: @whatsappbot:beeper.local
   static final RegExp beeperBotRegex = RegExp(r'^@.*bot.*:beeper\..*$');
 
-  /// Caches for performance optimization
-  static final Map<String, BeeperNetwork?> _networkCache = {};
-  static final Map<String, bool> _fakeDMCache = {};
-
-  static void clearCaches() {
-    _networkCache.clear();
-    _fakeDMCache.clear();
-  }
 
   /// Checks if a given matrix user ID belongs to a Beeper bridge bot.
   static bool isBeeperBot(String userId) {
     return beeperBotRegex.hasMatch(userId);
   }
@@ -27,63 +19,61 @@ class BeeperBridgeUtils {
   /// A Fake DM is defined as a room with exactly 3 members:
   /// 1. The local user (You)
   /// 2. The real contact
   /// 3. The Beeper bridge bot
   static bool isFakeDM(Room room) {
-    return _fakeDMCache.putIfAbsent(room.id, () {
-      // 0. Early exit: if the room has an explicit name set, it's a real group, not a fake DM.
-      final nameEvent = room.states['m.room.name'];
-      if (nameEvent != null && nameEvent.isNotEmpty) {
-        // Check the actual name content to make sure it's not empty/blank
-        final firstName = nameEvent.values.firstOrNull;
-        final roomName = firstName?.content.tryGet<String>('name') ?? '';
-        if (roomName.trim().isNotEmpty) {
-          return false;
-        }
+    // 0. Early exit: if the room has an explicit name set, it's a real group, not a fake DM.
+    final nameEvent = room.states['m.room.name'];
+    if (nameEvent != null && nameEvent.isNotEmpty) {
+      // Check the actual name content to make sure it's not empty/blank
+      final firstName = nameEvent.values.firstOrNull;
+      final roomName = firstName?.content.tryGet<String>('name') ?? '';
+      if (roomName.trim().isNotEmpty) {
+        return false;
       }
+    }
 
-      // 1. Check state event 'm.bridge'
-      final hasBridgeEvent = room.states['m.bridge']?.isNotEmpty ?? false;
-      if (hasBridgeEvent && room.isDirectChat) {
-        return true;
-      }
+    // 1. Check state event 'm.bridge'
+    final hasBridgeEvent = room.states['m.bridge']?.isNotEmpty ?? false;
+    if (hasBridgeEvent && room.isDirectChat) {
+      return true;
+    }
 
-      // 2. Check directChatMatrixID fallback
-      final directId = room.directChatMatrixID;
-      if (directId != null && directId.startsWith('@')) {
-        final localpart = directId.split(':').first.substring(1);
-        for (final entry in networkMap.entries) {
-          if (localpart.startsWith('${entry.key}_') || localpart == '${entry.key}bot') {
-            return true;
-          }
+    // 2. Check directChatMatrixID fallback
+    final directId = room.directChatMatrixID;
+    if (directId != null && directId.startsWith('@')) {
+      final localpart = directId.split(':').first.substring(1);
+      for (final entry in networkMap.entries) {
+        if (localpart.startsWith('${entry.key}_') || localpart == '${entry.key}bot') {
+          return true;
         }
       }
+    }
 
-      // 3. Fallback to standard 3-member bot check
-      final members = room.getParticipants();
-      if (members.length != 3) {
-        return false;
-      }
+    // 3. Fallback to standard 3-member bot check
+    final members = room.getParticipants();
+    if (members.length != 3) {
+      return false;
+    }
 
-      bool hasLocalUser = false;
-      bool hasBot = false;
-      bool hasRealContact = false;
+    bool hasLocalUser = false;
+    bool hasBot = false;
+    bool hasRealContact = false;
 
-      final clientUserId = room.client.userID;
+    final clientUserId = room.client.userID;
 
-      for (var member in members) {
-        if (member.id == clientUserId) {
-          hasLocalUser = true;
-        } else if (isBeeperBot(member.id)) {
-          hasBot = true;
-        } else {
-          hasRealContact = true;
-        }
+    for (var member in members) {
+      if (member.id == clientUserId) {
+        hasLocalUser = true;
+      } else if (isBeeperBot(member.id)) {
+        hasBot = true;
+      } else {
+        hasRealContact = true;
       }
+    }
 
-      return hasLocalUser && hasBot && hasRealContact;
-    });
+    return hasLocalUser && hasBot && hasRealContact;
   }
 
   /// Extracts the real contact from a Fake DM room.
   /// Returns null if it's not a Fake DM or if the contact cannot be found.
   static User? getRealContactFromFakeDM(Room room) {
@@ -134,17 +124,13 @@ class BeeperBridgeUtils {
     }
     return key;
   }
 
   /// Returns the detected Beeper network for a given room, or null if none is found.
+  /// Computed in real-time on every call to avoid caching stale state during initial sync.
   static BeeperNetwork? getRoomNetwork(Room room) {
-    if (_networkCache.containsKey(room.id)) {
-      return _networkCache[room.id];
-    }
-    final net = _detectRoomNetwork(room);
-    _networkCache[room.id] = net;
-    return net;
+    return _detectRoomNetwork(room);
   }
 
   static BeeperNetwork? _detectRoomNetwork(Room room) {
     final participants = room.getParticipants();
     for (final p in participants) {
